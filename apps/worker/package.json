{
  "name": "worker",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@terrasight/schemas": "workspace:*",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.80.0"
  }
}
