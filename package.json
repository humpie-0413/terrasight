{
  "name": "terrasight",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:worker": "pnpm --filter worker dev",
    "build:web": "pnpm --filter web build",
    "build:worker": "pnpm --filter worker build",
    "test:pipelines": "uv run pytest pipelines/tests",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
